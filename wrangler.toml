name = "subscription-notifier"
compatibility_date = "2024-01-01"

# Pages 部署配置
pages_build_output_dir = "public"

# D1 数据库绑定通过 Cloudflare Dashboard 配置
# 不要在此文件中填写 database_id
# 配置路径: Settings → Functions → D1 Database Bindings