name = "subscription-notifier"
compatibility_date = "2024-01-01"

# Pages 部署配置
pages_build_output_dir = "public"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "subscription-db"
database_id = "YOUR_DATABASE_ID"

# 定时触发器（在 Dashboard 中配置）
# [triggers]
# crons = ["0 9 * * *"]