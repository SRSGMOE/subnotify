name = "subscription-notifier"
main = "src/worker.js"
compatibility_date = "2024-05-01"
compatibility_flags = ["nodejs_compat"]

# 敏感配置通过 Dashboard 设置：
# 1. D1 绑定: Settings → Variables → D1 Database Bindings (变量名: DB)
# 2. 环境变量: Settings → Variables → Environment Variables
#    - ADMIN_PASSWORD (加密)
#    - TELEGRAM_BOT_TOKEN (加密)
#    - TELEGRAM_CHAT_ID (加密)